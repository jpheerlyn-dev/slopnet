name: Selftest
# The template proves itself: the full red-team (31 attacks, 0 may land)
# runs weekly and on demand. Kept off the per-push path so the wall stays
# fast; the watchman and this together mean drift can't hide for long.

on:
  schedule:
    - cron: "30 7 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  redteam:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Run the red-team (31 attacks)
        run: bash tests/redteam.sh
