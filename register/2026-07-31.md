# 2026-07-31

## 04:40 — claude (Fable) did

Ran the operator's own reference at last: `scripts/demo_agency.py` in the StormCode tree. I should have run it before drawing anything.

What it does that I was not doing. Every message is a full-width panel, not a bare line. Each panel opens with a header row carrying the speaker's logo, their name in bold, then the action icon and its label dimmed beside it. The body is wrapped and indented two columns. Most importantly `prow()` fills the vendor's background across the **entire** inner width of every row — border, fill, pad to width, border, one reset at the end. Mine tinted only behind the text, which is why the operator could see black gaps between the colour and the red edge.

One warning in that file worth keeping: the vendor face is applied to raw text only, never to an already-styled string, because an escape sequence contains letters and digits that would be remapped into striped glyphs and corrupt the sequence.

Done this pass:

- Coding-app tiles are two to a row instead of three, so a name and a status line both fit. Three was what made them read as buttons.
- Each tile now says whether that app is signed in and can build. There was no such state to read — the app knew only a static `tools.json` — so sign-ins are remembered in defaults when a wizard run finishes, and the board reads them back. Before this the tiles were five identical badges saying nothing.
- The person's message is a panel now, in the demo's shape: rule, header row with the drawn `user-message` icon and **You**, then the wrapped text.

Files: `packaging/SlopNetBrand.h/.m`, `packaging/SlopNetLauncher.m`, `tests/brand_striped_probe.m`, `MANIFEST.sha256`.

Evidence: a rendered sample read back with escapes stripped; striped probe 9, prompt probe 25, scroll probe 6, six checks, app builds.

**Not done, and it needs saying plainly.** The guide's reply is still a bare line, not a panel. It cannot be boxed as it stands: the reply arrives from a running process and a panel needs its closing border, so the output has to be collected and drawn when the run ends. That is a change to how the console handles a chat run, not a colour change, and I am not making it blind at the end of a long session. It is the next thing.

Ten of the fourteen action concepts are still unused, and the panels still do not carry the vendor face across their body text the way the demo does.
