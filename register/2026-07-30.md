# 2026-07-30


## 00:20 — hermes did

Copied StormCode terminal visuals into SlopNet for a later coding-agent integration. No originals deleted.

- Documents cold backup: `~/Documents/stormcode-prototype-backup-2026-07-30/` (stormcode minus .venv, terminal-styles, Desktop/logos, installed fonts) — ~178M
- SlopNet import: `packaging/terminal-visuals/` (~63M) with font-icons, action-icons, custom icons, built TTFs, build/demo scripts, runtime visual modules, terminal-styles, INVENTORY.json, README.md, AGENT_BRIEF.md
- Findings for operator: Poolside is in the font but omitted from `demo_swarm.py` roster; IBM Granite cubes SVG exists but was never mapped in `STEM_TO_PROVIDER`; operator wants multicolour Google dropped for now; full preview = `show_logos.py` / `show_panels.py` / `demo_showcase.py`
- Desktop `stormcode` left in place untouched (copy only)

## 01:15 — claude (Fable) did

Integrated the StormCode terminal visuals into the SlopNet Mac app per `packaging/terminal-visuals/AGENT_BRIEF.md`, and made `SlopNet-Logo.png` the app icon (operator request).

Fonts (worked in a scratchpad copy of the build scripts; `~/Desktop/stormcode` and `~/Documents/stormcode-prototype-backup-2026-07-30` untouched):

- Wired IBM Granite cubes as its own glyph: `IBM_granite_2_cubes_logo` → `ibm_granite` @ `U+E027`, appended after searxng `U+E026`; no existing PUA codepoint renumbered.
- Rebuilt `StormCodeIcons.ttf` → `Menlo-StormCode.ttf` → `Menlo-StormCode-Color.ttf` (38 provider badges incl. granite, 8 sbix strikes × 4 widths each) and refreshed them in `packaging/terminal-visuals/packaging-fonts/`. Regenerated `runtime-modules/icons.py`; updated `scripts/build_iconfont.py`, `scripts/build_icons.py`, `runtime-modules/app.py` (BRAND), `INVENTORY.json`, `README.md`.

App (files touched: `packaging/SlopNetBrand.h` + `.m` new, `packaging/SlopNetConsole.m`, `packaging/SlopNetLauncher.m`, `packaging/build_app.sh`):

- `SlopNetBrand` ports the FULL 38-provider GLYPH/MARK/display-name table (deliberately not the short `demo_swarm.py` roster) and maps SlopNet's real tools (codex→openai, claude→anthropic, kimi→moonshot, gemini→google, grok→xai) plus `ibm-granite/...` local models → `ibm_granite`; unknown ids get a neutral `•`.
- The app bundles `Menlo-StormCode-Color.ttf` and registers it process-only via CTFontManager; the console, welcome badge strip, model picker and local-model banners then show real full-colour logos. If the font is missing or fails to register, everything falls back to plain Unicode MARK characters — verified, no tofu.
- No multicolour per-letter Google wordmark anywhere; Gemini uses its single mark (operator decision #2).
- App icon now built from `SlopNet-Logo.png` via sips/iconutil, with the old generated icon as fallback when the PNG is absent.

Proof (PNGs under `packaging/terminal-visuals/docs-findings/proof-2026-07-30/`): `app-window-color-badges.png` (full strip, all 38 badges), `poolside-in-strip.png` + `granite-cubes-in-strip.png` (Poolside and Granite cubes rendering in the running app), `granite-in-model-picker.png`, `app-window-mark-fallback.png` (font removed, readable MARK strip), plus the raw sbix bitmaps extracted from the rebuilt font.

Licence note: the Menlo-derived TTFs and the rest of `packaging/terminal-visuals/` stay OUT of git (a pushed repo would be a standalone public download of a Menlo derivative, which the operator ruled out). `build_app.sh` treats the font as optional and says so when it is missing.

Countersign pending: another agent still needs to run `slopnet verify` per AGENTS.md rule 7.

- [01:12] commit d7cc3e7 by jpheerlyn-dev: "Show colour provider badges in the app console" (7 files)

## 01:55 — claude (Fable) did

Phase 2 of the terminal visuals: the in-app console now carries the StormCode visual system itself, not just logos. Operator feedback on Phase 1 was that only logos got through — no colour, no panels, no animation. All five gaps (G1–G4 plus the G5 intent) are addressed.

**G1 — the console was throwing colour away.** `consume:` ignored every SGR sequence (`default: break; // colours etc: ignore`) and `redraw` painted the whole buffer in one font and one colour. Rewrote the console to hold each line as an `NSMutableAttributedString` and store a pen (foreground, background, bold, dim, reverse) per run of text. It now parses reset, bold, dim, reverse, the named 8+8 colours, 256-colour (`38;5;N`) and 24-bit truecolor (`38;2;r;g;b` / `48;2;…`), emitting `NSForegroundColorAttributeName` and `NSBackgroundColorAttributeName` per run; the text view was switched to `richText`. Cursor and progress-overwrite behaviour (`putText`, CSI A/B/C/D/G/H/K/J) is preserved — an overwritten span takes the new pen while untouched spans keep theirs. Also fixed a pre-existing bug: a private-mode sequence such as `\033[?25l` (hide cursor) had its digits printed as literal text, so any program hiding its cursor left `25l` in the output. The child now also gets `COLORTERM=truecolor`, and the PTY is sized from the real console width with `TIOCSWINSZ`/`SIGWINCH` on resize instead of a hardcoded 100x30.

**G2 — brand palette and panels.** Ported the full BRAND table from `runtime-modules/app.py` into `SlopNetBrand`: panel/text/mark/tint per provider for all 38 ids. Added the shell palette (void `#000000`, crimson `#FF003C`, ink `#E8E8E8`, ghost `#666666`) and ANSI builders — `headerANSI:`, `panelANSIForProvider:…` (crimson frame, brand-filled rows, badge + display name, optional animated action row in the brand tint), `panelStripANSIForProviders:` (side-by-side lanes, which works because every panel row is exactly N columns and resets its own colours — the property `demo_agency.py` relies on), and `brandSheetANSIForProviders:`. The console field is now jet black with a crimson hairline frame. Panels are used for provider/status blocks only; ordinary log lines stay unpanelled.

  Granite panel hexes, as the brief asked me to document: `panel #000000`, `text #FFFFFF`, `tint #00AB23`. The cubes SVG carries its own green gradient (`#006116` → `#00AB23`) and the badge bitmap was rasterised on black by `build_colorfont.py`, so the panel must stay black or the badge would sit on a mismatched square. Black-panel providers are already part of the design system (xai, nous, nova, scale, stormcode), and the tint is the gradient's bright end.

**G3 — action frames were missing from the TTF.** Confirmed before rebuilding: the shipped `Menlo-StormCode-Color.ttf` had **zero** glyphs in `U+E900+` — only the 14 static action icons at `U+E0A0`. Ran `build_action_frames.py` in the scratch working copy: 70 frames across 14 concepts now exist as 2-cell (`U+EB00+`) and 3-cell (`U+EC00+`) twins. Verified the rebuild renumbered nothing — granite `U+E027`, poolside `U+E021` and the 62 IBM striped glyphs at `U+E800+` all survived. Ported the concept→frames map into `SlopNetBrand` and animate by swapping one codepoint on a 0.125s timer (the 8 fps `show_frames.py` uses). While a program runs the glyph lives in the console's status line (never scrolls, never fights the child's cursor); while idle it animates inside the ready block via a new token-based `noteReplaceable:` / `replaceLinesFromToken:` API that redraws specific stored lines. Tokens survive scrolling and are refused once their lines age out, which is how an animation learns to stop. Fallback is a plain spinner in the same column count — no tofu.

**G4 — the flat logo dump is gone.** `providerMarkStrip` (one line of all 38 logos) was a Phase-1 proof, not a product view. The ready view is now a crimson `SLOPNET` header, a filled Granite panel (badge, animated Think glyph, the real model id, "private · no API key · no open port"), then a `CODING APPS` strip of small panels built from the real `tools.json` ids on their own brand surfaces. It refits to the window and is reprinted after every run. The full 38-provider sheet moved behind a new **Providers** sidebar button, which also prints a 24-bit colour check.

**G5 — assessed, and the intent delivered without the port.** `colorart.py`'s ART is 8x4 half-block art per provider (~64 colour literals each, 37 providers) and has no `ibm_granite` entry, so porting it is neither cheap nor complete; a 4-row badge also cannot sit in a 1-row panel header. What matters is covered instead: with the font absent, panels keep their full truecolor brand fills and use Unicode marks, which is verified readable (see `font-fallback.png`). A real colorart port stays available as later work.

New verification, following the `tests/*_probe.m` convention: `tests/console_colour_probe.m` — 20 checks covering truecolor fg/bg, 256-colour, named colours, `\r` overwrite keeping the new colour, private-mode sequences leaving no digits, replaceable-block redraw, token refusal after `clear`, and a real child on the PTY proving its 24-bit output survives and it still sees `TERM=xterm-256color`. All 20 pass.

```
clang -fobjc-arc -framework AppKit -framework CoreText -I packaging \
  tests/console_colour_probe.m packaging/SlopNetConsole.m \
  packaging/SlopNetBrand.m -o /tmp/colour_probe && /tmp/colour_probe
```

Files changed: `packaging/SlopNetConsole.h`, `packaging/SlopNetConsole.m` (rewritten), `packaging/SlopNetBrand.h`, `packaging/SlopNetBrand.m`, `packaging/SlopNetLauncher.m`, `tests/console_colour_probe.m` (new). Untracked package: `packaging/terminal-visuals/packaging-fonts/Menlo-StormCode-Color.ttf` (rebuilt, now 7.6 MB with frames), `runtime-modules/action_frames.py` (regenerated), `scripts/build_action_frames.py` unchanged.

Proof, all captured from the real SlopNet app (never Terminal.app), under `packaging/terminal-visuals/docs-findings/proof-phase2-2026-07-30/`:

- `console-truecolor.png` — all 38 brands on their own truecolor fills plus the 24-bit gradient sweep
- `brand-panels.png` — the filled Granite panel with the real model id, above the Claude/ChatGPT/Moonshot/Gemini/Grok panel strip
- `action-anim.gif` + `action-anim-frame1..4.png` — the Think glyph changing in the running app
- `idle-status.png` — the default ready view: black field, crimson chrome, panels
- `font-fallback.png` — the same app with the bundled TTF deleted: Unicode marks, brand fills intact, no tofu

`SLOPNET_DEBUG_VISUALS=1` on the app binary opens the Providers view at launch; that is how the colour proofs were captured, and it is the quickest way to check whether the badge font and colour survived a build.

Known cosmetic limitation, not papered over: in fallback mode a few Unicode marks (⚔ ☰ ⬢) render slightly wider than one cell in the system monospace font, so the right frame of the providers sheet is a little ragged. Readability is unaffected, and the marks come from the established `icons.py` table, which I did not change.

Not touched: `~/Desktop/stormcode` and `~/Documents/stormcode-prototype-backup-2026-07-30` (all font work happened in a scratch copy). No existing PUA codepoint renumbered. No multicolour Google wordmark. No orchestrator logic ported.

Countersign pending: another agent still needs to run `slopnet verify` per AGENTS.md rule 7.

- [01:47] commit 2b6ea72 by jpheerlyn-dev: "Carry the StormCode visual system in the app console" (7 files)

## 01:49 — jpheerlyn-dev countersigned

COUNTERSIGN at commit 2b6ea72:

- PASS — walls (checks --all)
- FAIL — machinery manifest
- FAIL — doctor
- FAIL — red-team (no score)

Verdict: NOT verified — read the FAIL lines

## 01:50 — hermes countersigned

COUNTERSIGN at commit 2b6ea72:

- PASS — walls (checks --all)
- FAIL — machinery manifest
- FAIL — doctor
- FAIL — red-team (no score)

Verdict: NOT verified — read the FAIL lines

## 01:53 — hermes countersigned

COUNTERSIGN at commit 2b6ea72:

- PASS — walls (checks --all)
- PASS — machinery manifest
- PASS — doctor
- PASS — red-team (SCORE: 31/31)

Verdict: verified

## 01:54 — hermes did (independent review of 2b6ea72)

Countersign of Claude Fable's Phase 2 console visuals. Different agent from the author. Evidence re-run on this machine:

**Proofs re-run (not trusted on claim alone)**
- `clang … tests/console_colour_probe.m … && /tmp/colour_probe` → **COLOUR PROBE DONE — all ok** (truecolor fg/bg, 256-colour, named colours, `\r` overwrite keeps new pen, private-mode leaves no digits, replaceable-block redraw + colour, token refuse after clear, real PTY child with 24-bit + `TERM=xterm-256color`).
- `./slopnet check --all` → all six walls OK.
- `bash doctor.sh` → exit 0 (Docker noted as VPS-only ??, not a fail).
- `bash tests/redteam.sh` → **SCORE: 31/31** after hygiene below.
- `./slopnet verify --who hermes` → **Verdict: verified** at commit 2b6ea72 (see entry above).
- Font file `packaging/terminal-visuals/packaging-fonts/Menlo-StormCode-Color.ttf` present (~7.6 MB); cmap has granite `U+E027`, poolside `U+E021`, and **70** glyphs in `U+EB00+` action-frame range.
- Proof directory `packaging/terminal-visuals/docs-findings/proof-phase2-2026-07-30/` present (idle-status, brand-panels, console-truecolor, font-fallback, action-anim frames/gif).
- Code review: Console SGR pen + `NSBackgroundColorAttributeName`; Brand `panelANSIForProvider:`; Launcher ready block + 0.125s action timer + `noteReplaceable:`. G5 colorart skip accepted as documented (truecolor panel fills + MARK fallback).

**Honest earlier FAILs** at 01:49/01:50 stay in the register: working tree had stale MANIFEST (Console/Launcher changed in 2b6ea72 without machinery list update) and red-team baseline blocked while untracked `packaging/terminal-visuals/` (Menlo + .DS_Store haul) was tarred into the attack workspace.

**Hygiene committed with this countersign (not part of Claude's feature claim, required for honest PASS)**
- `update-manifest.sh`: add `packaging/SlopNetBrand.h/.m` and `tests/console_colour_probe.m` to the machinery set; regenerate `MANIFEST.sha256`.
- `.gitignore`: `packaging/terminal-visuals/` so Menlo derivatives and local visual assets are not a public git download and red-team can baseline.

**Not countersigned as product-complete**
- colorart half-block port still open (operator-accepted).
- Fallback wide Unicode marks still slightly ragged (documented cosmetic).
- Granite panel remains black by design (badge baked on black); green-filled panel would need font rebuild.

Desktop `stormcode` and Documents backup not touched.

- [01:54] commit a51c77e by jpheerlyn-dev: "Record Phase 2 console visuals independent verify" (5 files)
- [01:54] commit 36087dc by jpheerlyn-dev: "register: post-commit machine line for Phase 2 countersign" (1 files)
- [01:54] commit d418775 by jpheerlyn-dev: "register: post-commit machine line for countersign follow-up" (1 files)

## 02:13 — hermes did

Dock icon live hue + chroma while the app is running.

- `packaging/SlopNetLauncher.m`: load base AppIcon, 8 fps timer, CIColorControls (saturation breath 0.55–1.45, ~14s cycle) + CIHueAdjust (full hue walk ~48s), set `NSApp.applicationIconImage` each tick; restore base on terminate.
- `packaging/build_app.sh`: link CoreImage.
- Manifest regenerated for Launcher + build_app.

Compile check: clang of MacOS binary to /tmp/SlopNetDockTest exit 0.

- [02:13] commit 260be73 by jpheerlyn-dev: "Animate Dock icon hue and chroma while SlopNet runs" (4 files)

## 02:18 — hermes did

Dock icon white corners fix.

Root cause: `SlopNet-Logo.png` is an opaque white rectangle (corners ~254,254,254,255), not transparent. Static Dock mask hid it; live `applicationIconImage` showed the paper.

Fix: one-shot edge flood-fill of near-white background → alpha 0 before hue/chroma animation; keep CI source-in alpha mask per frame.

- [02:18] commit 426d8ad by jpheerlyn-dev: "Fix Dock icon white corners during hue animation" (3 files)
- [02:19] commit 01f1c24 by jpheerlyn-dev: "Fix compile of Dock white-corner punch (__block stack pointer)" (3 files)

## 02:24 — hermes did

Removed live Dock icon hue/chroma animation entirely.

Operator feedback: size wrong, corners wrong, lost system 3D lighting — looked hacky. Decision: not worth fixing now (replacing applicationIconImage fights Dock masking/lighting). Static AppIcon.icns only. CoreImage unlinked from build_app.sh.

- [02:24] commit 0854420 by jpheerlyn-dev: "Remove live Dock icon animation" (4 files)

## 02:50 — claude (Fable) did

Turned first-run setup into one linear in-app wizard, and finished IBM Granite as a real step of it (capacity → approve → install → proof → Chat unlocked) instead of a panel someone had to find in Settings.

What was already there, and deliberately reused rather than rebuilt: `slopnet-vps-local-helper.sh` already does the whole Granite sequence honestly — it explains the limits, asks once, then on the server prints free storage and memory, enforces the 5000 MiB / 6000 MiB floors for the default model, asks a second time before downloading, installs llama.cpp as the locked `slopnet` account, runs the bounded proof (`-c 4096 -b 512 -ub 256 --no-warmup`, 900s timeout) and writes `local-helper.env` only after the model answers `READY`. None of that was touched. The gap was that nothing led a beginner through it in order.

New files: `packaging/SlopNetWizard.h` / `.m` — a six-step sheet (Welcome, Server, Prepare the server, Private local guide, Coding app, You're ready) with Back where it is safe, and `tests/wizard_step_probe.m`. Changed: `packaging/SlopNetLauncher.m`, `packaging/SlopNetSettings.m`, `packaging/build_app.sh`, `update-manifest.sh`, `MANIFEST.sha256`.

How it behaves:

- **Long jobs stay in the console.** The wizard steps aside and the existing PTY runner does the work, because the onboard and local-helper scripts ask real questions there (a server password, "Install and test it?"). When a run exits 0 the launcher reopens the wizard at the next step, so nothing has to be found again. Nothing tells a beginner to open Terminal.
- **Capacity is shown before approval.** The guide step runs a read-only SSH probe (runtime account, llama, configured model, free disk, free memory, GGUF cache) and states the numbers. The Install button starts disabled and only enables after the server has answered — there is no path to a download that skips the check. The floors are duplicated in the app with a comment tying them to the script, so a person is told their server is too small before approving rather than half way through.
- **Resumable.** A prepared server is not prepared twice and a proved guide is not downloaded again: with `model` + `llama yes` the step flips to "Installed and proved: <model>" and offers Continue.
- **Chat is gated on the proof, not on the server.** Chat mode now refuses until the guide has passed, and says which step is missing instead of failing on the server. It still cannot start a planner, agents, or a build. Build's copy stays honest about the approved-build runner gap.
- **Discoverable without Settings**: a "Setup guide" sidebar row reopens the wizard at the resume step. Settings keeps its full local-guide panel for anyone who prefers it — nothing was removed — but its headings no longer say "Step 1 of 2" / "Step 2 of 2", because two competing numbering schemes is the confusion the wizard exists to remove.

Two new `NSUserDefaults` keys, in the spirit of the existing ones: `SlopNetGuideReady` (the guide passed its READY proof; set only from a clean local-helper run or from reading the model back off the server — a failed network read is never treated as evidence the guide is gone) and `SlopNetWizardDone` (the last screen has been seen, so the wizard stops opening itself). Both are cleared by "Forget this server", since what was proved belonged to that server.

Commands run:

```
./packaging/build_app.sh   # succeeds, now compiling SlopNetWizard.m
./update-manifest.sh       # adds SlopNetWizard.h/.m and tests/wizard_step_probe.m
clang … tests/wizard_step_probe.m packaging/SlopNetWizard.m && /tmp/wizard_probe
clang … tests/console_colour_probe.m … && /tmp/colour_probe
clang … tests/settings_resize_probe.m … && /tmp/resize_probe
```

**Proved on this machine** (`packaging/terminal-visuals/docs-findings/proof-wizard-2026-07-30/`):

- `cold-first-launch-step1.png` — defaults cleared, real app: the wizard opens itself at "Step 1 of 5 — welcome", not an empty console. The operator's real connection settings were exported first and restored immediately afterwards; verified identical.
- `resume-at-guide-step4-proved.png` — real app against the live VPS: resumed straight to step 4, ran the capacity probe, found the guide already proved, and offered Continue instead of a download.
- `live-capacity-probe.txt` — the same read-only probe against the live VPS: `runtime yes`, `llama yes`, `model ibm-granite/granite-4.1-3b-GGUF:Q4_K_M`, 236,589 MiB free disk, 22,609 MiB free memory. Host redacted.
- `tests/wizard_step_probe.m` — 23 checks, all passing: resume rules for all three states, every screen laid out at four window sizes including smaller than it opens (no constraint conflicts), install refused before a capacity check, a disabled install button reaching no delegate, the default model being the bounded Granite 3B, the 8B not offered, an already-prepared server not prepared twice, a proved guide not re-downloaded, and the last screen naming Chat, Build and the approved-build limit.
- Phase 2 probes re-run as regressions: colour probe all ok, settings resize probe ok.

**Not proved, and not claimed:** I did not re-run a live Granite download and READY proof from the wizard. That path was already proved by the product on this VPS (v0.8/v0.9 register entries), and this server already has the guide installed, so a fresh install proof would need either a wiped runtime account or a second server. I also could not click through the wizard as a person would — this environment has no accessibility permission for synthetic input, and granting it is a system security setting I will not change. The step machine, layout, gating and resume logic are therefore proved by the probe and by two real screenshots of the running app, not by a human-style click-through. A live end-to-end install on a fresh VPS is still worth doing before release.

Out of scope and untouched, as briefed: the project-specific VPS runner (approved-build still refuses honestly), multi-provider onboarding, README, branch protection, font rebuilds. No Dock icon animation was reintroduced. `~/Desktop/stormcode` and the Documents backup were not touched, and `packaging/terminal-visuals/` stays gitignored.

Countersign pending: another agent still needs to run `slopnet verify` per AGENTS.md rule 7.
- [02:47] commit 31a4fff by jpheerlyn-dev: "Make first run one wizard, with Granite as a real step" (9 files)

## 03:00 — glm-5.2 (coding agent) did

Added an `improvements/` reference folder for the coding agent: a prioritized plan (`README.md`) plus three not-wired-in snippets — `manifest_guard.sh` (commit-time MANIFEST check), `test_crew_scaffold.py` (std-lib unit tests for `crew.py`), `typed_crew_snippet.py` (type-hint examples). Verified: all 10 scaffold tests pass, the new files pass all six walls when staged, and the guard reports no stale-machinery change on the current tree. Analysis only; no machinery changed, so no `MANIFEST.sha256` update. Per AGENTS.md rule 5 these are reference notes the operator may keep, archive, or move to an orbit repo. Not committed (operator decides). Countersign pending (rule 7): another agent should run `slopnet verify`.

## 03:34 — glm-5.2 (coding agent) did

Observations and review notes only — no structural changes, no orbits created, nothing committed, per the operator's instruction. Added two review files to `improvements/`: `roadmap-gui-and-orbit.md` (the hacker-vibe GUI reskin plan + the orbit roadmap with safety flags and sequencing) and `hacker-skin-snippets.m` (Objective-C chrome snippets: phosphor palette, chiseled bezel, scanline overlay — explicitly marked to keep the console's child output authentic). Verified: both pass all six walls when staged. Key advice recorded: reskin is trunk-scope; `vps-files` is a safe first orbit; `tool-market`/`granite-foreman` need sandboxed installs and double-confirm; `crew-bridge` (Hermes/OpenClaw) stays deferred behind the project-runner proof; and a read-only HTTP "web door" at `127.0.0.1:8000` is the hidden prerequisite before any orbit can integrate. Countersign pending (rule 7).

## 03:49 — hermes did

Added operator-priority improvement notes for coding agents:

- `improvements/hermes-next/` — PRIORITY.md (biggest: project Build runner), FIND_ME.md, snippets (project_runner_contract.py, build_gate_mac.m, wizard_console_status.m, manifest_guard_precommit.sh)
- Pointed `improvements/README.md` at hermes-next as the product track vs secondary wall/GUI notes

Reference only — not wired into machinery. Not committed unless operator asks.

## 03:10 — claude (Fable) did (review of improvements/, no code changed)

Operator asked for a review of the glm-5.2 and Hermes notes under `improvements/` and a recommendation on what to do first. Nothing was implemented and nothing under `improvements/` was committed — it is still untracked, exactly as found. The only change in this commit is this register entry.

**Recommendation: (1) the project-specific Build runner, then (2) the `crew.py` unit tests.**

Item 1 is not a close call. `jobs/OPERATORS.md` "Immediate next proof" names it, `register/PENDING_OPERATOR.md` carries the Codex entry forbidding any end-to-end claim until it passes, and both note-sets now agree (glm-5.2's README points at `hermes-next/` for the product track). It is also the only item that removes a dead end a real user can reach: the wizard now gets someone all the way to a saved plan, and "Start approved build" then correctly refuses. `snippets/project_runner_contract.py` is a sound acceptance sketch — cage path check, plan artifact required, second confirm required, walls before merge — and `snippets/build_gate_mac.m` correctly says to keep today's refusal until the VPS side is real.

Item 2 is my own pick over the more obvious "manifest guard", because it is the only item on either list that makes item 1 *safer* rather than tidier. The runner has to reuse the walls logic — `refuse_fake_gate` (blocks "always pass" test commands) and `parse_waves` (rejects shared files and placeholder paths) — and that logic currently has no fast tests, only the bash red-team. Locking it before the runner edits it is the right order. `improvements/test_crew_scaffold.py` runs on the standard library alone: I ran it, **10/10 pass**.

**Where I disagree with the notes, with evidence:**

- **The GUI reskin is not a trunk priority now.** `roadmap-gui-and-orbit.md` Part 1 calls it "the one piece that is fully trunk-legal" and sequences it first. That contradicts `OPERATORS.md`'s immediate next proof, and Hermes's own item 7 defers the full reskin. Its hard rules (don't repaint live console output, one phosphor identity, scanlines as overlay only) are good and worth keeping *for when it is scheduled*.
- **"`doctor.sh` only advises on branch protection; it never fails" is not accurate.** It calls `bad` — a real failure that sets `failed` and changes the exit code — when it can query the API and finds no required checks. It reports `[??]` via `unknown` (which does *not* affect the exit code) only when `gh` is missing or unauthenticated. So the honest fix is narrower than the note says: make a missing/unauthenticated `gh` fail rather than pass quietly, instead of rewriting the whole check.
- **The `classify_failure` leak is real but smaller than stated.** `_short_failure` (crew.py:579) does return the raw last stderr line truncated to 240 chars with no redaction, so an unmatched auth error can echo a token. But I traced both call sites (crew.py:750, 800): the value goes to `crew_fail`, which prints and exits. It is not persisted, and the Mac app's history deliberately never records console output. So this is a display-surface risk worth a 3-line fix, not a committed-secret hole — it should not outrank the runner.
- **Hermes item 4 ("console as status for wizard + build") is already largely done.** After Phase 2 and `31a4fff` the wizard's Granite step already prints `headerANSI:` plus a Granite `panelANSIForProvider:` during install, the ready view is one panel plus a small strip rather than a 38-logo dump, and the action glyph animates only while work runs. Re-spending here would be redundant.
- **Both manifest-guard snippets have defects; if one is wired, wire glm-5.2's after a one-line fix.** Tested both against a synthetic stale manifest in a throwaway repo:
  - `improvements/manifest_guard.sh` prints the proper RULE/WHY/FIX, reads the machinery list out of the manifest itself, and accepts `--all`, so it fits the `checks/*.sh` contract. **But its `case "$changed" in *"$f"*)` substring match is over-eager:** with a stale manifest, staging only `notes/about-slopnet.md` trips it, because the manifest entry `slopnet` is a substring of that path. In this repo that means most paths (`packaging/slopnet-vps-*.sh`, `.slopnet/`, …) would trigger a warning about machinery nobody touched, defeating the guard's own stated intent. Fix is one line — exact-line match, e.g. `printf '%s\n' "$changed" | grep -Fxq "$f"`.
  - `snippets/manifest_guard_precommit.sh` fails with a bare `sha256sum: WARNING: 1 computed checksum did NOT match` and no RULE/WHY/FIX, so it breaks the wall contract and tells the person nothing about what to do; it also hardcodes machinery prefixes instead of reading the manifest, so it will drift from `update-manifest.sh`.
  - Worth stating plainly: this guard is ergonomics, not a security boundary — `git commit --no-verify` skips it like every other local wall. Its real evidence is good though: today's register shows two consecutive countersigns failing on "machinery manifest" before hermes fixed it.
- **`snippets/wizard_console_status.m` will not compile against trunk.** It calls `headerANSI:` (real API is `headerANSI:width:`), `panelANSIForProvider:lines:width:` (real API is `panelANSIForProvider:title:detail:action:frame:width:`) and `console.columnCount` (real property is `columns`). Directionally right, stale in detail.

**Walls check, as asked.** Staged the whole `improvements/` tree and ran all six: `secrets`, `protected-paths`, `naming`, `junk`, `slop-lint`, `register` — **all six pass**. Two notes on why: the tree contains `improvements/hermes-next/snippets/__pycache__/project_runner_contract.cpython-312.pyc`, which `git add -A` never staged because `.gitignore:6` covers `__pycache__/`; when I force-added it, `checks/junk.sh` caught it and failed correctly. Both layers work. I then unstaged everything, so the tree is as I found it.

**The hidden dependency, confirmed.** `SLOPNET.md` (lines 137 and 189) requires orbits to talk to the main app over HTTP at `http://127.0.0.1:8000`, reached from a Mac by SSH forwarding. No such listener exists — the only served surface is `slopnet mcp` over stdio (`cmd_mcp`, slopnet:1539). So every orbit row in `roadmap-gui-and-orbit.md` is blocked on building that door first. My own caution to add: that door is a new listening socket on the VPS, which is the one thing the Granite work was careful never to open, so it needs the same discipline — loopback bind only, SSH forwarding, read-only to start, and its own proof — rather than being treated as a small freebie.

Order I would put to the operator: (1) project runner + walls template; (2) `crew.py` unit tests, wired before the runner touches that code; (3) manifest guard with the substring fix; (4) the `_short_failure` redaction; (5) the narrow `doctor.sh` `gh`-missing fix; then the web door, and only then orbits and any reskin. `crew-bridge` stays hard-deferred per `OPERATORS.md`.

Also outstanding and not mine to close: commit `31a4fff` (the wizard) is still uncountersigned, and per AGENTS.md rule 7 I cannot countersign my own work. That, plus a fresh Granite install on a clean runtime account, remains the honest end-to-end gap.

Countersign pending: a different agent needs to run `slopnet verify` for both `31a4fff` and this review entry.

- [03:58] commit ad1546c by jpheerlyn-dev: "register: review of improvements/ notes, no code changed" (1 files)

## 04:20 — claude (Fable) did

Acted on my own review: did the two prep items I said should land *before* the project runner, and stopped there. The runner itself is not started — see the end of this entry for why.

**A task may now only own files inside the project.** Probing `parse_waves` before adopting the test scaffold, I found it accepted `../../etc/passwd`, `/etc/passwd`, `~/.ssh/authorized_keys` and `.git/config` with no validation at all. Traced the consequence honestly before fixing: `task["files"]` is used for the placeholder check, the emptiness check, the wave-clash check, and — the part that matters — it is injected into the agent prompt under "Only touch the files listed under Files:". It never opens or writes a path itself, so containment today rests on the sandbox and the worktree, not on this parse. That makes this missing defence in depth rather than an open hole in the local engine, but it is exactly the property the project runner's cage will lean on, and the plan is written by a model, so `_escapes_project` now refuses a path that climbs out (`..`), starts at the root, expands a home directory, carries a drive letter or UNC prefix, or reaches into `.git`. Backslashes count as separators so a Windows-style path cannot smuggle a `..` past a `/`-only split. Ordinary work is untouched: `.gitignore`, `.github/workflows/ci.yml`, `./src/main.py` and nested paths all still parse.

**Failure lines are redacted instead of dropped.** `_short_failure` returned the agent's raw last line, unredacted, and `improvements/README.md` item 5 proposed replacing it with a fixed phrase. I checked where that line goes first: the agent runs with `stdout=PIPE, stderr=PIPE` (crew.py ~721), so its output is captured and never streamed — the `reason` line is the only clue a person gets. A fixed phrase would have traded a leak for a blind failure. So `_redact` masks the secret shapes instead and keeps the diagnosis: AWS keys, GitHub `ghp_`/`github_pat_`, `sk-`, Slack `xox*`, JWTs, `Bearer`/`Basic` values, credentials inside a URL, and `key: value` for api_key/secret/token/password/authorization. Benign lines are unchanged — verified that `ModuleNotFoundError`, `npm ERR! code E404`, a git pathspec error and `token count: 4096` all survive intact, since over-redaction would be its own failure.

**New `tests/crew_unit_probe.py`** — adapted from glm-5.2's `improvements/test_crew_scaffold.py` (credited in the file), extended with the path-escape and redaction cases. 16 checks, standard library only, runs in under a second: `python3 tests/crew_unit_probe.py`. This is the item I argued should land before the runner touches this code, because the runner has to reuse `refuse_fake_gate` and `parse_waves`.

Worth recording because it is the wall doing its job: my first version of that test embedded the fake credentials as literals, and `checks/secrets.sh` (gitleaks) blocked the red-team's baseline commit — `generic-api-key`, `tests/crew_unit_probe.py` lines 157 and 164. The fix is the same trick `tests/redteam.sh` already uses for its fake AWS key: assemble each fake secret from harmless halves at run time. A test about not leaking secrets should not ship a secret shape.

Also corrected one of my own findings from the 03:10 review. I reported `doctor.sh` exiting 2; that was my invocation, not the repo. `doctor.sh` declares `#!/usr/bin/env bash` and line 190 uses a bash process substitution, so `sh doctor.sh` is a syntax error. `slopnet` invokes it with `bash` (slopnet:407), and `bash doctor.sh` exits 0. No repo bug, and nothing in the earlier entry needs changing.

Files: `crew.py`, `tests/crew_unit_probe.py` (new), `update-manifest.sh`, `MANIFEST.sha256`.

Commands run and results:

```
python3 tests/crew_unit_probe.py     # 16/16 ok
sh tests/redteam.sh                  # SCORE: 31/31 — no regression from the crew.py change
for c in checks/*.sh; do sh "$c"; done   # all six pass
bash doctor.sh                       # exit 0
./update-manifest.sh                 # crew.py + tests/crew_unit_probe.py now covered
```

**What I deliberately did not do, and why:**

- **The project runner (my own item 1).** Building it is in scope and wanted, but `OPERATORS.md` asks for it to be built *and proved*, and the proof means running coding agents on the operator's VPS against a paid subscription. That is the operator's spend and their server, so it needs an explicit go-ahead rather than an agent deciding for them. Shipping an unproved runner into trunk would also create exactly the "looks integrated, isn't proven" state this project has avoided — the Mac side correctly refuses today, and `snippets/build_gate_mac.m` says to keep that refusal until the VPS side is real. Ready to start on a word.
- **The commit-time manifest guard.** Both note-sets say wire it only when the operator schedules it, and I flagged it as ergonomics rather than a boundary (`--no-verify` skips it like every local wall). It also needs the substring fix I recorded at 03:10 before it is worth wiring.
- **The `doctor.sh` `gh` fix.** I looked at it properly and decided against doing it unilaterally: making a missing or unauthenticated `gh` fail would change who can countersign, since `doctor.sh` feeds the verify ritual. Blocking countersigns to close an advisory gap is the operator's call, not mine — especially with `31a4fff` still waiting on one.

Countersign pending: a different agent needs to run `slopnet verify`. Still outstanding from earlier and not mine to close — `31a4fff` (the wizard) is uncountersigned, and a fresh Granite install on a clean runtime account is still the honest end-to-end gap.
