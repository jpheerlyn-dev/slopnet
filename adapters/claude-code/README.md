Copy this folder's contents into your project as `.claude/` (`cp -r adapters/claude-code/. .claude/`).
Prompts then auto-log to `register/`; paths in `PROTECTED.txt` become
un-editable for the agent. Keep `.claude/settings.local.json` out of git
for personal overrides.
